#include<stdio.h>
int main()
{
    while(1)
    {
        int row,col,end;
        printf("Enter : ");
        scanf("%d",&end);

        for (row=1; row<=end; row++)
        {
            for (col=1; col<=row; col++)
            {

            }
        }


        getch();
    }
}
